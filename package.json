{
  "name": "render-agentkit-hacker-news-agent",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  },
  "pnpm": {
    "overrides": {
      "zod": "^4.0.0"
    }
  }
}
